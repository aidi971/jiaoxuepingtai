<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langheng.modules.ed.dao.LessonDao">
	
	<!-- 查询数据
	<select id="findList" resultType="Lesson">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="findAllSysFile" resultType="java.util.Map">
		SELECT file_id as fileId, file_name as fileName, file_type as fileType FROM sys_file WHERE status != '1'
	</select>

	<select id="findAllTestGather" resultType="java.util.Map">
		SELECT gather_id as fileId , gather_name as fileName, test_Type as fileType FROM ed_test_gather WHERE status != "1"
	</select>

	<!-- 查询数据 -->
	<select id="findListHadPush" resultType="com.langheng.modules.ed.entity.Lesson">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
			AND a.lesson_id in (SELECT lesson_id FROM ed_teaching_class_lesson AS tcl  WHERE  tcl.teaching_class_id=#{teachingClassId}  )
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select>

</mapper>